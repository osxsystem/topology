# Rust build output
/gatekeeper/target/

# Installer/provisioner output — prebuilt binaries placed here at install time, never committed
/bin/

# macOS
.DS_Store

# Claude Code ephemeral state
.claude/worktrees/
.claude/scheduled_tasks.lock

# docs/memory/ is committed source (framework-repo handoff artifacts, ADR-0013).
# Governed-project handoffs land under .claude/topology/memory/ in the project repo, not here.

# shadow burn-in log (raw stream is local; triage notes are committed)
docs/logs/

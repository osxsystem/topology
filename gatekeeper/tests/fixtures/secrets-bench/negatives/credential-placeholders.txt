# placeholder credentials, as found in templates and docs — the direct FP canary for the
# labeled-assignment rule: values here are too short, the wrong charset, or absent entirely
password = "changeme"
password: ********
passwd = "hunter2"
api_key: <YOUR_API_KEY>
api-key = "TODO"
auth_token = ""
auth-token: REPLACE_ME
Authorization: Bearer <token>

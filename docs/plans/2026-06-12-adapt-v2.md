# Plan — `adapt` v2: project integration (Phase 9)

Executes the [spec](../specs/2026-06-12-adapt-v2.md); grounded in the
[research note](../research/2026-06-12-adapt-v2.md) and ROADMAP Phase 9.
Branch: `feat/adapt-v2` (worktree `topology-phase9`). Coding delegated to a Sonnet subagent; the
main loop plans and reviews. TDD: every task lands its failing test first (`#[ignore]` or red),
then the production code that un-ignores/greens it.

**Protected / sensitive paths this phase:** `gatekeeper/src/main.rs` (dispatch-table usage string for
`adapt`, if touched), `templates/CONTRACT.template.md` (regenerates `AGENTS.md` — Phase 10
byte-equality test must stay green), `gatekeeper/src/adapt.rs` (the work). None are on the scan
integrity list (rules.toml/hooks/scanner/settings), so no `--no-verify` override is expected; if a
commit trips the scan hook, document the override in the commit message per the Track 2 grant.

**Baseline (plan gate):** `cargo test` on `feat/adapt-v2` (fresh off `main`) must be green before
task 1. Verified: see verify artifact.

| # | Task | Files | Acceptance |
|---|------|-------|------------|
| 1 | **Red unit fixtures for the two partial-file primitives.** `#[cfg(test)]` cases in `adapt.rs` for `ensure_import_line` (None→create; present→Unchanged; absent→append preserving content) and `ensure_managed_block` (no-block→append; identical→Unchanged; differing→replace-in-place leaving outside content; malformed→error). Plus `merge_claude_settings` cases (preserve user `model` key; set `env.GATEKEEPER_BIN`; non-object existing→Err). All reference not-yet-existing fns → compile-red. | `gatekeeper/src/adapt.rs` (tests) | `cargo test` fails to compile / red on the new cases; pre-existing tests untouched |
| 2 | **Implement the primitives.** `Edit` enum (`Unchanged\|Created\|Updated\|Failed`); `ensure_import_line`, `ensure_managed_block` (markers per spec §1b, fail-closed on malformed), `merge_claude_settings` (spec §2, preserves all non-`hooks`/non-`env.GATEKEEPER_BIN` keys). Pure, no I/O. | `gatekeeper/src/adapt.rs` | task-1 unit tests green; `cargo clippy -- -D warnings` + `cargo fmt --check` clean |
| 3 | **Red integration fixtures** in `gatekeeper/tests/cli_adapt.rs` (reuse the `run`/scratch-root + project-git idiom), `#[ignore]`-tagged: AC-1 append-only, AC-2 create-if-missing, AC-3 codex block round-trip, AC-4 settings no-clobber, AC-5 GATEKEEPER_BIN value, AC-6 scaffold dirs, AC-7 bare-stub instruction in rendered CONTRACT.md, AC-8 malformed block→exit 2. | `gatekeeper/tests/cli_adapt.rs` | force-running the `#[ignore]` tests fails against current code; default suite green |
| 4 | **Wire settings.json merge into `build_claude` + apply path.** `build_claude` no longer emits a whole-file settings.json; `cmd_adapt` reads existing settings, calls `merge_claude_settings` with `bin = <read_root>/bin/gatekeeper`, writes/check via a settings-aware step. Update the existing `claude_wires_both_hooks` / `adapt_writes_to_project_not_framework` tests for the merge (hooks still present; framework-root paths still referenced). | `gatekeeper/src/adapt.rs`, `gatekeeper/tests/cli_adapt.rs` | AC-4, AC-5 green; existing hook tests green; `--check` clean after write |
| 5 | **Scaffold + contract write on the roots-differ branch.** Add `.claude/topology/{research,specs,plans,verify,reviews}/.gitkeep` GenFiles; render `project_ctx()` contract → `.topology/CONTRACT.md` GenFile; for `--harness claude` append the `@.topology/CONTRACT.md` import via `ensure_import_line`; for `--harness codex` upsert the managed block via `ensure_managed_block`. Guard so a print-only `--contract` invocation does none of this. | `gatekeeper/src/adapt.rs` | AC-1, AC-2, AC-3, AC-6, AC-8 green; idempotent (`--check` exit 0 after write) |
| 6 | **Bare-stub first-session instruction in the template.** Add the gate-shaped section (spec §4) to `templates/CONTRACT.template.md`; regenerate `AGENTS.md` (`adapt --contract framework` output) so the Phase 10 byte-equality test stays green. | `templates/CONTRACT.template.md`, `AGENTS.md` | AC-7 green; Phase 10 `AGENTS.md` byte-equality test green |
| 7 | **Usage + docs.** Update the `adapt` usage synopsis in the Phase 14 dispatch table (no hand-written `USAGE_*`) to name the project-integration behavior; add a Phase 9 / Phase 9.1 follow-up line to USER-GUIDE.md and ROADMAP status; CHANGELOG entry. | `gatekeeper/src/main.rs`, `docs/USER-GUIDE.md`, `docs/ROADMAP.md`, `CHANGELOG.md` | `cli_doc_sync` test green; `gatekeeper check docs` ok |
| 8 | **Version bump + full suite.** Bump `gatekeeper/Cargo.toml` to the next minor; `cargo test`, `cargo clippy -- -D warnings`, `cargo fmt --check`, `just test-payload`/`test-e2e` if touched, all green. | `gatekeeper/Cargo.toml`, `CHANGELOG.md` | finish gate (`gatekeeper check finish -- cargo test`) green |

**Sequencing:** 1→2 (primitives, pure, fastest). 3 seeds all integration ACs red. 4 and 5 are the
two production edits (settings merge; scaffold+contract); 4 before 5 since 5's contract write and
the settings merge share the roots-differ branch. 6 is template-only (independent, but lands before
finish so the AGENTS.md regen is in one commit). 7–8 close out. Each task is one commit (test commit
may precede its production commit where the tdd gate counts production paths).

**Out of scope (per spec §0):** cursor/opencode native-surface import, `adapt --init-agent`,
global-scope contract delivery — carried to Phase 9.1.

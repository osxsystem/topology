VERDICT: pass
HEAD: df0153e257bddb9a9822c8a04c5cf39f07d3c5ea
BASE: bff7265e47a51c368b997d7cd99572f57920fd84

# Review: installer-v2 (2026-06-10)

## Blocking findings
None.

## Resolved findings (fixed by df0153e since the fdcca56 review)
- Migration block missing `mkdir -p` — fixed: README.md:120 and docs/USER-GUIDE.md:112 now lead the one-time migration with `mkdir -p .claude/topology` before the five `git mv` lines. Re-reproduced live in a scratch git repo: all five moves now succeed (previously `git mv docs/research .claude/topology/research` aborted with `fatal: renaming ... No such file or directory`).
- Swallowed `adapt` failure — fixed: scripts/install.sh:307-311 wraps the capture in `if ! WIRING_OUTPUT="$(...)"`, so a non-zero `adapt` no longer dies silently under `set -e`; the captured diagnostics are echoed to stderr followed by `error: harness wiring failed (adapt --harness $HARNESS)` and `exit 1`. Re-proved live (scratch project with `.claude` pre-created as a regular file, `file://` release fixture): installer printed `gatekeeper adapt: cannot create .../.claude: File exists (os error 17)` + the named error, exit 1. The success path is unchanged: a full `--project <scratch> --harness claude --yes` run with the fixture still exits 0, writes `<scratch>/.claude/settings.json`, lists it in the manifest, and reaches the doctor close-out.
- Silent positional swallow — fixed: scripts/install.sh:84-87 collapses the old `-*` reject + `*) shift` swallow into a single `*)` arm that rejects any unrecognized argument (flag-shaped or positional) with `error: unexpected argument '$1'`, usage, exit 2. Spot-ran `bash scripts/install.sh bogus` → usage + exit 2. Grepped every documented/internal invocation of install.sh (README, USER-GUIDE, specs, plans, hooks): none passes positionals, so nothing relied on the old swallow; `--harness`/`--project` missing-value errors are intact above the catch-all.
- EOF abort at the replace prompt — fixed: scripts/install.sh:402 `read -r ans < "$PROMPT_INPUT_FD" || ans=""` — Ctrl-D at the stale-binary prompt now leaves `ans` empty, takes the "kept ... cp to update manually" branch (install.sh:407), and the script continues to the manifest and doctor close-out instead of dying under `set -e`. Pattern verified under `set -euo pipefail` (read-from-/dev/null harness continues past the failed read).
- Fix-commit scope check: `git show df0153e` touches only README.md, docs/USER-GUIDE.md, scripts/install.sh (+10/-5); every hunk maps to one of the four findings; `shellcheck scripts/install.sh` clean at HEAD; no Rust, hooks, or skill files touched, so the fdcca56 test/gate evidence below still stands.

## Non-blocking notes
- gatekeeper/src/adapt.rs:189-195 (codex) and 239-245 (opencode) — in a governed project these adapters generate configs that rely on a project-root `AGENTS.md` (Codex auto-discovery; opencode `"instructions": ["AGENTS.md", ...]`) which adapt never writes into the project, so the operating contract only actually reaches claude (framework-rooted hook paths) and cursor (contract embedded in the .mdc). No AC covers cross-project codex/opencode wiring, so this is a follow-up gap, not a regression.
- gatekeeper/src/doctor.rs:168-179 — `probe_version` now executes whatever `gatekeeper` is first on PATH (the base version only printed its path). Consistent with the installer's existing trust of local PATH binaries (the smoke test does the same), but it is a new execution of an attacker-nameable file; worth a one-line threat-model note if doctor is ever run in untrusted checkouts.
- scripts/install.sh:336-349 — the harness=none heredoc still prints the legacy string-form `"UserPromptSubmit": "<path>"` wiring (install.sh:342 at HEAD) while `build_claude` (adapt.rs:269-281) emits the array-of-matcher-groups schema. Pre-existing at BASE (scripts/install.sh:131 on bff7265), correctly left untouched for diff traceability — but the two now disagree inside one feature surface; align in a follow-up.
- scripts/install.sh:265 — the manifest entry `hooks/*.sh scripts/*.sh (made executable)` is relative while every other entry is an absolute path; cosmetic inconsistency in the printed audit list.
- Release availability: the bumped pin (0.3.0 in .claude-plugin/plugin.json:3) points at a GitHub release that will only exist after the post-merge tag; until then every fresh install takes the cargo-build fallback. Disclosed in docs/verify/2026-06-10-installer-v2.md:74-80 — acceptable transition, flagging so the tag is not forgotten.

## Criteria checked
### Spec/plan
- AC-1 (framework repo unchanged) — re-ran `check research|design|plan|verify --feature installer-v2` and `check docs` with gatekeeper/target/release/gatekeeper (0.3.0) inside the worktree: all PASS against root `docs/` (equal-roots branch in gatekeeper/src/main.rs:177-187); no artifact files moved in this repo per `git diff --stat bff7265..HEAD`. df0153e touches no Rust or artifact paths.
- AC-2 (external artifacts) — re-executed live in a scratch git repo with TOPOLOGY_ROOT: `check design` FAILs naming `.claude/topology/research`; placing research+spec under `.claude/topology/{research,specs}/` flips to PASS; the same files under scratch root `docs/` are not found (`check plan` still FAILs). Matches gatekeeper/tests/cli_check.rs:367-474. The documented migration onto this layout now actually executes (df0153e, first resolved finding).
- AC-3 (review gate on the project repo) — re-executed live: a well-formed artifact at `.claude/topology/reviews/2026-06-10-x.md` pinning the scratch HEAD passes; a dirty tracked non-reviews file fails with `uncommitted changes outside .claude/topology/reviews/`. gatekeeper/tests/cli_review.rs:126-170 covers both directions; framework-repo fixtures unchanged (review.rs gate_tests use `arts(&root)` = `docs/`).
- AC-4 (adapt writes to the project) — re-executed live: `adapt --harness claude` from a scratch repo wrote `<scratch>/.claude/settings.json` with both hook commands pointing at the framework dir's `hooks/*.sh`; `ls -a` of the framework showed nothing new; `adapt --check` passed immediately after. gatekeeper/tests/cli_adapt.rs:157-230 asserts the same. After df0153e, an adapt failure during install is also surfaced loudly with exit 1 instead of a silent death (re-proved live, second resolved finding).
- AC-5 (prompted install) — re-executed at df0153e: `install.sh --project <scratch> --harness claude --yes` non-tty with a `file://` release fixture: vendored framework at `<scratch>/.topology`, appended `.topology/` to the scratch `.gitignore`, manifest listed vendored framework, `.gitignore` edit, binary, CLAUDE.md symlink, pre-commit copy, and the wiring file (scripts/install.sh:200-210, 312-319), exit 0. `--global --harness none --yes` on a disposable clone reproduced the v1 flow at fdcca56, and df0153e does not touch that path. Arg parsing now rejects all unrecognized arguments with usage + exit 2 (plan Task 6 "reject unknown flags with usage" — df0153e closes the positional hole, third resolved finding).
- AC-6 (stale-PATH repair) — re-executed at fdcca56 with a 0.0.1 stub prepended to PATH: non-tty run printed the warning naming path, `stale: 0.0.1`, `installed: 0.3.0`, and the `cp` remedy, and `cmp` proved the stub byte-identical; a pty run (`script(1)`, answer `y` via the PROMPT_INPUT_FD seam) overwrote the stub in place and the manifest recorded `(overwritten with 0.3.0)`. With `--yes` the repair takes the warn-only branch (scripts/install.sh:391) — no overwrite without an explicit interactive yes; the only mutation is `cp` over the same path (install.sh:404), never a delete. df0153e additionally makes EOF at the prompt equivalent to "no" (install.sh:402, fourth resolved finding) so a Ctrl-D no longer skips the manifest/doctor close-out.
- AC-7 (doctor) — `doctor` prints `framework root:`, `project root:`, `artifacts root:` (doctor.rs:21-26) and with the 0.0.1 stub on PATH appended `(version skew: 0.0.1 vs 0.3.0)` while exiting 0 (`failures` not incremented, doctor.rs:61-77); gatekeeper/tests/cli_doctor.rs:111-179 asserts both. Re-observed in the df0153e end-to-end run (skew note against the 0.1.0 cargo-bin gatekeeper, doctor still `all probes ok`).
- AC-8 (quality gates) — at fdcca56: `cargo test` 232 passed, 0 failed; `just check` green end-to-end (fmt, clippy, tests, shellcheck, typos, `check docs: ok`). At df0153e (shell + docs only): `shellcheck scripts/install.sh` clean, `bash scripts/install.sh bogus` exits 2 with usage, full fixture install exits 0 — Rust surface untouched so the cargo evidence carries over. Resolution logic unit-tested via pure helpers `resolve_project_root`/`resolve_artifacts_root` with distinct tempdirs and no `env::set_var` (main.rs:724-825).
- Review-gate hardening survived the signature change (read at HEAD, not just the diff): rename/copy porcelain entries remain fail-closed (review.rs:208-214 — re-proved live with a `git mv` out of `.claude/topology/reviews/` vetoing the gate); the clean-path filter is anchored to exactly the active reviews relpath derived via `strip_prefix` with a forced trailing slash so `docs/reviewsX` cannot match (review.rs:246-257); `--base` option-shape validation intact (review.rs:289-292); `--untracked-files=all`, unreadable-candidate veto, and ambiguity veto all present (review.rs:263-271, 318-327, 370-379).
- resolve_project_root edge cases — `.git` file (worktree) accepted (main.rs:152-163, test main.rs:743-757); no-`.git` falls back to start; canonicalize-fallback equality in resolve_artifacts_root (main.rs:178-181). `git clone --local` vendoring from a linked worktree verified working live (and re-exercised in the df0153e fixture run).
- Version coherence — `.claude-plugin/plugin.json:3`, `.claude-plugin/marketplace.json:12`, `gatekeeper/Cargo.toml:3` all say 0.3.0; Cargo.lock matches; `--version` prints `gatekeeper 0.3.0 (rules schema v1)`.

### Standards
- Diff traceability (AGENTS.md "Diff traceability") — every hunk in `git diff bff7265..fdcca56` maps to a spec/plan task: two resolvers + threading (main.rs), gate_review/cmd_adapt signature widening (review.rs, adapt.rs), doctor roots+skew (doctor.rs), installer prompts/scope/repair (install.sh), docs/skills (Task 7), version bump (verify finding). The df0153e hunks map 1:1 to review findings 1/2/4/5 and nothing else. scan.rs, hooks/, instinct.rs, learn.rs, memory.rs untouched; the pre-existing string-form heredoc was correctly left alone. No unrequested refactors found.
- Simplicity (AGENTS.md "Simplicity") — no speculative knobs: the artifacts root is conditional, not configurable (no env override, per ADR-0012 decision 2, main.rs:177-187); PROMPT_INPUT_FD and TOPOLOGY_RELEASE_BASE_URL are plan-specified test seams, not feature flags; the legacy `docs/reviews/` strip_prefix fallback (review.rs:256) is the only arguably-dead branch and is fail-safe. The df0153e fixes are minimal-shape (one `mkdir -p` line, one `if !` wrap, one case-arm merge, one `|| ans=""`).
- Three language lanes — Rust changes confined to the gatekeeper crate (fmt/clippy clean via `just check`); install.sh keeps `set -euo pipefail` (line 25) and passes shellcheck at df0153e; skill edits are one-line description changes that keep the house format (verb phrase + "Use when", e.g. skills/write-plan/SKILL.md:3).
- Docs match behavior, no overclaims — doctor's skew note documented as informational (USER-GUIDE: "reports a version skew note informally (exit 0)" matches doctor.rs not incrementing failures); the artifacts rule is stated conditionally everywhere ("docs/ here, .claude/topology/ in a governed project" in all seven skill descriptions; "The framework repo itself keeps its root docs/ layout unchanged" in README.md and USER-GUIDE); the stale-PATH text ("offered an in-place overwrite", "no deletion") matches the `cp`-only implementation. The one docs/behavior mismatch found at fdcca56 (the migration block) is fixed by df0153e and re-verified working.
- ADR-0012 conformance — two named roots implemented exactly as decided (framework anchoring for scan/instinct/learn/memory/check docs unchanged in main.rs:63-68; project anchoring for gates/review/adapt); doctor prints both roots plus the artifacts root so mis-resolution is observable in one command (ADR consequence, doctor.rs:21-26); installer asks with flag twins and prompts only via /dev/tty (install.sh:42-55).

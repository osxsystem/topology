VERDICT: pass
HEAD: e6cb91415beaa3f603659925e9d63f9a1fb55157
BASE: bc73b31506403cd0c4d039b16e3657945301c39e

# Review: adapt v2 — project integration (Phase 9) (2026-06-12)

## Blocking findings
None.

## Non-blocking notes
- `gatekeeper/tests/cli_root_resolution.rs:71` — the `#[allow(dead_code)]` on the pre-existing
  `gk_bin()` is the surgical-changes-respecting choice (annotate, don't delete pre-existing dead
  code), but it is technically adjacent to the plan's tasks. Mentioning, not blocking: a one-line
  allow on a genuinely-unused helper (confirmed: only the definition matches at both base and HEAD)
  is the lowest-churn way to keep `clippy -D warnings` green and is acceptable.
- `gatekeeper/src/adapt.rs:910-961` — for cursor/opencode project installs the scaffold and
  `.topology/CONTRACT.md` are written, but the contract is not imported into their native surface
  (the `match` only handles `claude`/`codex`; cursor/opencode fall through `_ => {}`). This is the
  spec §0 deferral to Phase 9.1 and is genuinely out (no half-done import wiring), so it is correct;
  the only nuance is that those harnesses get the rendered contract file on disk without a pointer to
  it yet — harmless and additive to fix later.
- `gatekeeper/src/adapt.rs:835-857` — a malformed (unparsable) existing `.claude/settings.json`
  causes `--check` to exit 2 before the import/scaffold/contract checks run. This is fail-closed and
  defensible (a corrupt settings file is a hard error), but means `--check` does not surface
  *additional* drift in the same run. Acceptable; noted for completeness.

## Criteria checked
### Spec/plan
- **AC-1 (append-only CLAUDE.md).** `ensure_import_line` (adapt.rs:30-51) creates with the bare line
  when missing, no-ops on a trimmed match, else appends preserving prior bytes + single trailing
  newline. `ac1_append_only_claude_md` (cli_adapt.rs:325-366) asserts prior bytes survive, import is
  appended, second run is byte-identical, and `--check` exits 0. Real assertions. PASS.
- **AC-2 (create-if-missing).** `ac2_create_if_missing_claude_md` (cli_adapt.rs:421-442) asserts the
  file equals exactly `@.topology/CONTRACT.md\n`. PASS.
- **AC-3 (codex managed-block round-trip).** `ensure_managed_block` (adapt.rs:62-136) appends when
  absent, no-ops on byte-identical body, replaces in place preserving outside content. Idempotency
  traced: written as `BEGIN\n{body}\n END\n`, body re-extracted equals `body` exactly → Unchanged on
  re-run. `ac3_codex_managed_block_round_trip` (cli_adapt.rs:448-506) tampers inside the block →
  `--check` exit 1, re-run restores it, outside prose survives, outside edit preserved. PASS.
- **AC-4 (settings no-clobber).** `merge_claude_settings` (adapt.rs:145-177) preserves all non-`hooks`
  / non-`env.GATEKEEPER_BIN` keys; unit tests at adapt.rs:1393-1420 plus integration
  `ac4_settings_no_clobber` (cli_adapt.rs:509-549) assert `model` survives and `--check` does not flag
  it. The `--check` drift predicate (adapt.rs:860-871) compares only the two managed keys. PASS.
- **AC-5 (GATEKEEPER_BIN value).** `ac5_gatekeeper_bin_value` (cli_adapt.rs:552-576) asserts the value
  equals `<framework>/bin/gatekeeper`; live-session proof is correctly deferred to Phase 12 per spec.
  PASS.
- **AC-6 (scaffold).** Five `.claude/topology/*/.gitkeep` GenFiles emitted on the roots-differ branch
  (adapt.rs:915-920); `ac6_scaffold_dirs_exist` (cli_adapt.rs:579-597) asserts all five dirs. PASS.
- **AC-7 (bare-stub instruction + Phase 10 byte-equality).** Template gains the section
  (templates/CONTRACT.template.md); `ac7_contract_contains_first_session_instruction`
  (cli_adapt.rs:600-618) asserts it in the rendered project contract. Verified independently:
  `gatekeeper adapt --contract framework` is byte-identical to the committed `AGENTS.md`. PASS.
- **AC-8 (fail-closed managed block).** `ensure_managed_block` returns `Failed` on begin-without-end
  and duplicate-begin (adapt.rs:71-80); `apply_edits` maps `Failed` → exit 2 naming the file
  (adapt.rs:238-246). `ac8_malformed_managed_block_exits_2` (cli_adapt.rs:621-645) asserts exit 2 with
  non-empty stderr. PASS.
- **§0 scope deferrals genuinely out.** No `--init-agent` symbol anywhere (grep clean); cursor/opencode
  native-surface import absent (not half-done). PASS.
- **Hollow-`--check` regression bound.** `cmd_adapt` carries the settings verdict in `settings_code`
  and aggregates instead of early-returning (adapt.rs:815-968). `ac1c_check_detects_removed_import`
  (cli_adapt.rs:371-397) and `ac6b_check_detects_missing_contract` (cli_adapt.rs:399-418) delete the
  import / contract and assert `--check` exit 1 — they bind directly to the documented hollow-gate bug
  and would pass falsely (exit 0) against the pre-fix early-return. Real regression tests. PASS.

### Standards
- **ADR-0007 (no new runtime deps).** `Cargo.lock` diff is solely the `0.8.0`→`0.9.0` version bump;
  dependency count unchanged (25 → 25). `serde_json` (already adopted) is the only JSON path. PASS.
- **ADR-0014 (dispatch-table usage).** No hand-written `USAGE_*` consts in `gatekeeper/src` (grep
  clean); the `adapt` synopsis is updated in the `SubcommandSpec` dispatch table (main.rs). PASS.
- **Append-only / never-clobber.** Both partial-file editors are pure over (existing, payload) and
  only ever append or replace the marker-delimited region; `merge_claude_settings` preserves unknown
  keys. Whole-file ownership retained only for adapt-owned payload files (`.topology/CONTRACT.md`,
  `.gitkeep`). PASS.
- **Fail-closed posture.** Malformed managed block → exit 2; unparsable settings.json → exit 2;
  unresolved template placeholder → exit 2; `--check` never writes. PASS.
- **Surgical changes.** Diff reads as the plan's tasks: primitives + tests, settings merge, scaffold +
  contract wiring, template/AGENTS.md section, version bump, docs. The lone arguably-adjacent change
  (`#[allow(dead_code)]`) is the minimal warnings-clean choice and is noted above. PASS.
- **Quality gates.** `cargo test` (73 across suites + lib) green; `cargo clippy --all-targets
  -- -D warnings` clean; `cargo fmt --check` clean (re-run in this review). PASS.
